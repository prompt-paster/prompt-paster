# Agent Plan

## Current System State

- Active branch: `roadmap-post-v1-feedback`.
- Last shipped milestone: `M10 - Manual QA fixes` / overlay Settings button is merged.
- Current repository state: SwiftPM macOS menu-bar app with prompt storage,
  seed prompt library, searchable overlay browser, copy-and-close selection,
  app bundle and DMG packaging, fallback global hotkey, double-Control trigger,
  Settings window, launch-at-login wiring, native text editing shortcuts in the
  overlay search field, and an in-overlay Settings button.
- Current task: `ROADMAP-1: Add post-v1 feedback roadmap`.
- Current blockers: no code blocker. Signing/notarization remains undecided for
  public distribution.

## Active Task Breakdown

- [x] Create concise static agent instructions in `AGENTS.md`.
- [x] Create high-density repository sitemap in `llms.txt`.
- [x] Create immediate state tracker in `.agent-plan.md`.
- [x] Open and merge `CONTEXT-1` agent-context architecture PR.
- [x] Merge shipped product slice: `CLIPBOARD-1: Add copy-and-close selection loop`.
- [x] Merge shipped product slice: `HOTKEY-1: Add fallback global hotkey`.
- [x] Merge shipped product slice: `HOTKEY-2: Add double-control trigger and permission flow`.
- [x] Merge shipped product slice: `SETTINGS-1: Add settings and launch-at-login controls`.
- [x] Implement current product slice: `PACKAGING-1: Add installable app packaging`.
- [x] Validate release packaging artifact.
- [x] Open a labeled, non-draft PR: #10.
- [x] Merge release-QA fix: #11 `Cmd+A` in overlay search.
- [x] Merge release-QA fix: #12 in-overlay Settings button.
- [ ] Merge roadmap update: `ROADMAP-1`.

## Context Pointers

- Product/design details: `docs/design-architecture-spec.md`.
- Implementation roadmap and PR sequence: `docs/implementation-plan.md`.
- Prior shell review rationale: `docs/review-recommendations.md`.
- Architecture index for agents: `llms.txt`.
- Static execution rules: `AGENTS.md`.
- Release packaging scripts: `scripts/build-dmg.sh` and
  `scripts/validate-release-package.sh`.

## Next Work Routing

The initial v1 implementation sequence is complete. The next implementation PR
should follow the post-v1 roadmap in `docs/implementation-plan.md`; current
recommended next slice is `UI-QA-1: Make menu-bar icon reliably visible`.
